function navigateTo(action) {
    if (action === 'ok') {
        window.location.href = 'https://example.com/ok';
    } else if (action === 'cancel') {
        window.location.href = 'https://example.com/cancel';
    }
}